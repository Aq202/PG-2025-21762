    <resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Public_transport_app" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/main_color</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>

    <style name="Theme.Public_transport_app" parent="Base.Theme.Public_transport_app" />

    <style name="ToolbarTheme" parent="ThemeOverlay.Material3.ActionBar">
        <item name="titleTextColor">@color/white</item>
        <item name="subtitleTextColor">@color/white</item>
    </style>

    </resources>